## Annotator for CodexLeaks Project
1. Install rust with `rustup`.
2. Setup `.env` file with proper `DATABASE_URL` variable (optional: github api token for github search api calls), run:
```bash
cargo run
```
